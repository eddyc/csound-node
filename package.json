{
    "name": "csound-wasm-test",
    "version": "0.1.5",
    "main": "dist/index.js",
    "dependencies": {
        "@babel/runtime": "^7.7.2",
        "audioworklet-polyfill": "^1.1.2"
    },
    "scripts": {
        "start": "react-scripts start",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "build": "rimraf dist && NODE_ENV=production babel src/wasm/Csound.js --extensions \".js\"  --copy-files --ignore __tests__,spec.js,test.js,__snapshots__ -o dist/index.js && cp src/wasm/types.js dist/types.js"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "@babel/cli": "^7.7.0",
        "@babel/core": "^7.7.2",
        "raw.macro": "^0.3.0",
        "react": "^16.11.0",
        "react-dom": "^16.11.0",
        "react-scripts": "3.2.0",
        "rimraf": "^3.0.0"
    }
}
